name: "🚀 執行每日金融晨報 AI 流水線"

# 🎯 這是 GitHub 官方唯一的「手動觸發按鈕」語法，這段絕對不能少
on:
  workflow_dispatch:
    inputs:
      news_text:
        description: '請輸入今日待處理的金融新聞文字內容'
        required: true
        default: '請在此貼上今天的新聞正文...'

jobs:
  execute-agentflow:
    runs-on: ubuntu-latest
    steps:
      - name: "📥 1. 下載專案原始碼"
        uses: actions/checkout@v4
        with:
          persist-credentials: true
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: "🐍 2. 設定 Python 環境"
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: "📦 3. 安裝 NextData AgentFlow 核心套件"
        run: |
          python -m pip install --upgrade pip
          # 💡 自動安裝框架所需的依賴度
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: "⚙️ 4. 發動你的原始 AI 工作流"
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          INPUT_NEWS_TEXT: ${{ github.event.inputs.news_text }}
        run: |
          # 💡 這行才是實質驅動你原本配置檔的 Python 引擎指令
          python main.py --config .github/workflows/morning.yml --text "$INPUT_NEWS_TEXT"

      - name: "💾 5. 自動將產出的 Word 檔案變更回傳至 GitHub"
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git fetch origin
          git add *.docx || true
          if ! git diff --cached --quiet; then
            git commit -m "✨ AI自動更新：產出今日金融晨報 Word 檔 [skip ci]"
            git push origin HEAD:${{ github.ref }}
          fi
