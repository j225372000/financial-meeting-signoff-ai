name: morning_test
description: 測試 YAML 驅動的 Morning Workflow，含 Data Mapping

steps:
  - id: write_draft
    skill: writer
    input:
      classify_result: memory.classify_result
      extract_result: memory.extract_result
    output:
      result: memory.draft_prompt
    prompt: templates/morning/writer_prompt.txt
